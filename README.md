# SpareTrip Cloudflare Pages source

This folder is the public deployment source for Cloudflare Pages.

Build settings:

- Framework preset: None
- Build command: empty
- Build output directory: /
- Root directory: cloudflare-pages, if this folder is committed inside a larger repository

Do not deploy the repository root. The repository root contains operational reports and server scripts.
